namespace RobotStudioUnityBridge;

// Placeholder entry point for the RobotStudio -> Unity export pipeline.
// Design pending: RobotStudio Add-in (in-process, uses the Bin-net48 ABB.Robotics.* API)
// vs. standalone application (reads a saved/Pack&Go station). Target framework and API
// references will follow from that decision.
public static class ExportPipeline
{
}
